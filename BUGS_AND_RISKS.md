# EggSpend — Bugs & Risks Register

Working register for the agent loop. Full analysis with reproduction detail: `docs/audit-and-roadmap.md` §4–§5. Status values: open / in-progress / fixed(date, commit) / accepted-risk.

| # | Sev | Summary | Location | Fix task | Status |
|---|-----|---------|----------|----------|--------|
| B1 | Critical | Liability sign bug adds debt to net worth in Metrics timeline & Monthly Review | `MetricsView.swift:89`, `MonthlyReviewCalculator.swift:113` | T1 | fixed(2026-07-08) |
| B2 | High | Four divergent net-worth formulas; `includeInNetWorth` inconsistently respected | Dashboard/NetWorth/Metrics/MonthlyReview | T1 | fixed(2026-07-08) |
| B3 | High | Historical net-worth reconstruction reverses account-less transactions | `MonthlyReviewCalculator.swift:112`, `MetricsView.swift:88` | T2 | fixed(2026-07-08) |
| B4 | High | CSV transaction import can't link accounts, never updates balances | `CSVImportView.swift:363-408` | T8 | fixed(2026-07-09) |
| B5 | High | Amount entry broken in comma-decimal locales (`Double(amountText)`) | `AddTransactionView.swift:62` et al. | T5 | fixed(2026-07-08) |
| B6 | High | Currency hard-coded USD (78 sites) | app-wide | T12 | fixed(2026-07-09) |
| B7 | High | CloudKit multi-device: duplicate generated recurring transactions, double balance application | `RecurringTransaction.swift:132-151` | T23 | open |
| B8 | Medium | CSV type inference defaults unknown types to income; "credit" ambiguity | `CSVParser.swift:238-247` | T3 | fixed(2026-07-08) |
| B9 | Medium | `parseAmount` strips `-` mid-string; trailing-minus ignored; EU formats misparse | `CSVParser.swift:94-108` | T3 | fixed(2026-07-08) |
| B10 | Medium | Ended recurring items never materialize final overdue occurrences | `RecurringTransaction.swift:138` | T4 | fixed(2026-07-08) |
| B11 | Medium | Account deletion: no confirmation; orphans corrupt history; goals silently zero | `NetWorthView.swift:182-186` | T7 | fixed(2026-07-08) |
| B12 | Medium | Account edit silently rewrites balance from stale form state | `AddAccountView.swift:110-121` | T10 | fixed(2026-07-09) |
| B13 | Medium | Infinite-loop risk if `advanceNextDueDate` fails to advance | `RecurringTransaction.swift:108-141` | T4 | fixed(2026-07-08) |
| B14 | Medium | Budget alert state mutations may not be saved; CloudKit merge can dupe alerts | `Budget.swift:189-206`, `BudgetAlertCoordinator.swift` | T6 | fixed(2026-07-08) |
| B15 | Medium | O(n²) transaction grouping; non-localized section headers | `TransactionsListView.swift:85-97` | T21 | open |
| B16 | Medium | Forecast/SafeSpend double-count recurring due today + drift overlap | `ForecastEngine.swift`, `RecurringProjection.swift:36-42` | T15 | fixed(2026-07-09) |
| B17 | Low | Savings-rate/percent truncation via `Int(...)` | `MetricsView.swift:444` | (minor, batch with T12) | open |
| B18 | Low | `Double` money math app-wide (documented) | app-wide | accepted for now | accepted-risk |
| B19 | Low | Weekly budget period anchored to locale week | `Budget.swift:101-114` | backlog | open |
| B20 | Low | Empty Metrics/Forecast charts for new users | `MetricsView.swift`, forecast views | T22 | open |
| B21 | Medium | Transfers between included and excluded accounts change net worth but are invisible to historical reconstruction | `NetWorthCalculator.swift:at()` | T14 (snapshots bound damage for post-launch dates); backlog (pre-snapshot dates) | open (bounded by T14) |
| B22 | Medium | Recurring transactions linked to archived accounts keep materializing and mutating its balance silently | `RecurringTransaction.swift:144-173`, `Account.isArchived` | T7+ (needs product decision) | open |
| B23 | Low | Forecast upcoming events / calendar recurring occurrences not filtered by archived account; inconsistent with bills filter | `ForecastEngine.swift:61-74`, `CashFlowCalendarView.swift:24-25` | T7+ follow-up | open |
| B24 | Medium | CSV import duplicate detection uses Double exact-equality for amount key; future Decimal migration must update the key | `CSVImportView.swift` (TransactionDuplicateKey) | T12+ (Decimal refactor) | open |

## Risks (non-bug)
| # | Risk | Mitigation task |
|---|------|-----------------|
| R1 | `try?` saves swallow financial-data write failures | T6 — fixed(2026-07-08); note: recurring-failure UI surfacing remains backlog item |
| R2 | Silent CloudKit→local fallback; user may believe data is backed up | surface SyncStatus (SettingsView is dedicated host, awaits R2 as its own task) | open |
| R3 | No app lock | T20 |
| R4 | No data export/portability | T13a/T13b | fixed(2026-07-09) — DataExporter engine + SettingsView with ShareLink export UI; CSV (transactions/accounts/transfers) and versioned JSON backup accessible via gear icon on Home tab |
| R5 | No CI — regressions land unnoticed | T11 | fixed(2026-07-09) — CI green confirmed (Tests run 16 on commit 56edc99); workflow passed on GitHub runner for first time |
| R6 | Manual `generate_project.py`/pbxproj sync — drift breaks builds | QA step 3 checks every loop |
| R7 | Unparseable balance text now creates visible full-delta adjustment | T10 (mitigation: form guard against parse-failure saves) | open — improved visibility vs. silent-zero, but new surface for user error (UX hardening follow-up) |
| R8 | Account type-flip (asset↔liability) during edit yields odd-looking adjustment ledger entry | T10 (product-copy consideration) | open — numerically correct but may confuse users (e.g. changing a checking account to credit card creates a historical balance-correction transaction) |
| R9 | CurrencyFormat.override is a bare static var with no SwiftUI dependency edge | T12+ (SettingsView is dedicated host, awaits R9 as its own task with @Observable/notification wiring) | open — Settings toggle must add @Observable/notification wiring to trigger re-render when override changes; currently requires app restart to see new currency display |
| R10 | Subscription detection uses unbounded transaction history; single anomalous charge (e.g. trial price 0.99 mid-history) permanently rejects entire cluster | T16a `SubscriptionDetector.detect()` allSatisfy gate; T16b call-site wiring | resolved(2026-07-09) — 36-MONTH call-site input window (RecurringTransactionsView toolbar entry point passes sliced transaction history), combined with engine's historical-only stability gate (all-but-latest occurrences), resolves outlier-poisoning risk (false-negative rate negligible for typical user histories under 3 years) |

**Note (2026-07-08):** B1/B2 fixed via new shared `NetWorthCalculator` (`EggSpend/Utilities/NetWorthCalculator.swift`), adopted by DashboardView, NetWorthView, MetricsView, and MonthlyReviewCalculator. Metrics and Monthly Review now respect `includeInNetWorth` like Dashboard/NetWorthView already did. B3 (historical reconstruction reversing account-less transactions) fixed via T2: new `NetWorthCalculator.at(date:accounts:transactions:)` method filters to account-linked transactions only. B21 (transfer-boundary limitation) discovered in T2 code review and registered as separate Medium-severity item for future resolution. B8/B9 fixed via T3: rewritten `parseAmount` with sign-first detection (paren, leading, trailing, U+2212), embedded-minus rejection, unchanged thousands/decimal logic; CSV type inference now uses expense/income keyword lists with amount-sign fallback, whole-word token matching for short keywords (pos/dr/cr); QA caught "pos" substring collision in "deposit" on round 1 (revision applied); round 2 pass with Python-oracle verification of all 16 type + 13 amount test cases. B10/B13 fixed via T4: removed item-level `endDate < now` skip permitting ended items to materialize final overdue occurrences; added previousDueDate capture + print-and-break guard when advanceNextDueDate() fails to advance, preventing infinite loop on Calendar.nextDate() nil returns; test fixture revised to nextDueDate = now−14 days for deterministic behavior across all month lengths; QA round 1 failed on date flakiness (fixture off-by-one), implementer revised and QA round 2 passed. B5 fixed via T5: new `AmountParser.swift` utility with fast-path `Double(text)` and position-validated grouping fallback (single decimal separator, non-empty all-digit fraction, grouping candidates {., comma, space U+00A0 U+202F} minus decimalSep, first group 1–3 digits, subsequent exactly 3); swapped all parse/pre-fill sites across 6 views (AddTransactionView, AddAccountView, TransactionFilterView); QA round 1 found fr_FR test used wrong grouping separator (ICU uses U+202F not dot) and 100x hazard ("12,50" in en_US could mis-parse as 1250 if grouping stripped comma naively); implementer applied position-validated logic to reject this case; QA round 2 passed with Python verification of full 17-case matrix and no regression on negatives. B14/R1 fixed via T6: CSVImportView now catches save failures and displays errorMessage instead of silently advancing to .done; failed saves rollback only their own tracked inserts (safe no-op on untracked objects, does not discard unrelated edits); BudgetAlertCoordinator now explicitly saves alert-dedupe state; processRecurringTransactions returns Bool (failures logged to console); PersistenceController.seedDefaultCategoriesIfNeeded returns Bool on save. QA round 1 caught data-integrity gap (failed save keeps user on .preview, retry would double-insert); implementer round 2 added rollback. Code review requested changes: rollback() could discard unrelated unsaved edits from other screens. Implementer round 3 replaced with targeted deletes. Code review approved round 3 (full path coverage, safe no-op for untracked objects). Follow-up: processRecurringTransactions Bool return unread by both onAppear callers; recurring failures reach console only (UI surfacing deferred to future task). B11 fixed via T7: Account.isArchived flag added (CloudKit-safe, default false); archive UX via swipe action on NetWorthView with confirmation dialog; archived accounts excluded from net-worth calculations (NetWorthCalculator.current/totals), forecasts (ForecastEngine.liquidBalance, CashFlowCalendarView.bills), and four pickers (with keep-selected pattern for existing references); TransactionFilterView deliberately unfiltered to preserve history access; four separate confirmation steps for destructive delete. Code review round 1 flagged swipe action unsafe (Delete first on trailing edge, full swipe would trigger destructive dialog); implementer revised to Unarchive on leading edge (allowsFullSwipe: true) and Delete alone trailing (allowsFullSwipe: false), per CategoryManagementView precedent. B22/B23 discovered during T7 as scope-deferred risks: recurring transactions linked to archived accounts continue materializing (needs product decision); forecast upcoming events not filtered by archived (inconsistent with bills filter). First real local `xcodebuild test` run this loop: full suite TEST SUCCEEDED, 299 tests, all 5 new T7 tests pass, retroactively confirming all prior Phase-0 loop verdicts (T1–T6, T11).

**Note (2026-07-09, post-T8):** B4 fixed via T8: CSVImportView.swift now includes Account Section on map step (transactions-only, persisted as selectedImportAccount), unarchived Picker with None default, and applyBalanceToggle (default true). Repo Analyst flagged CRITICAL hazard: selectedImportAccount being pre-persisted and not covered by T6's targeted deletes meant failed saves could orphan dirty account balances and retries would double-apply. Implementer added symmetric AccountBalanceService.reverse() in the catch block BEFORE the targeted deletes, ensuring balanced reversal regardless of save outcome. Net-effect sentence on preview step shows (transactions && account selected && toggle on), deriving wording from amount signedness and formatting with .currency(code: "USD"). 4 new tests (testNetBalanceEffectSumsOnlyValidRowsWithSign, testImportingExpensesWithToggleOnReducesAccountBalance, testImportingExpensesWithToggleOffLeavesBalanceUnchangedButLinksAccount, testReversingAllImportedTransactionsRestoresOriginalBalance) trace concrete arithmetic end-to-end. QA PASS: TEST SUCCEEDED 303/303 (299→303 from T7). Code review APPROVE with zero required fixes, 5 optional follow-ups filed: (1) netBalanceEffect relocation to Utilities for cleanup; (2) suppress net-effect sentence when effect = 0; (3) liability account wording (negative = owed) may be debt-inverted for users — product-copy decision; (4) document no-back-navigation invariant from .preview to .map to protect reversal-correctness invariant; (5) tests pin primitives not performImport view method — future guard-drops in performImport not caught (remember for T9).

**Note (2026-07-09, loop 10 T9):** T9 (CSV import duplicate detection) completed QA PASS + Code Review APPROVE, zero required fixes. User resolved product decision: exact same-day duplicate = same calendar day + same amount + case/whitespace-normalized title + same account (when set); no ±1-day window, no per-row override in v1. CSVParser.swift adds normalizedTitle property (lowercase/trim/collapse whitespace). CSVImportView.swift gains file-scoped TransactionDuplicateKey (Hashable: startOfDay day + positive Double amount + normalizedTitle + optional accountID). New methods: duplicateKey(), markDuplicates(in:existingKeys:accountID:) with growing seen-set (invalid rows skip marking). buildPreview fetches all transactions unfiltered (deliberate: includes isGenerated recurring-materialized txs — bank row colliding with materialized recurring charge is a real duplicate). isValid→willImport switch in netBalanceEffect, summaryBar counts, Import button; third summaryBar bucket shows duplicate count. TransactionPreviewRow treats "Duplicate — will skip" (dimmed via willImport). performImport skips isDuplicate AFTER isValid guard BEFORE insert/apply (duplicates never reach insertedTransactions → T8 reversal symmetry preserved). doneStep shows "N duplicates skipped" line gated on >0. 8 new tests all pass (311/311). Code review filed 3 optional follow-ups: (1) doc-comment clarifying nil-account bucket (not ignore-account-when-none); (2) B24 NEW: Double exact-equality in key — future Decimal migration must update; (3) unfiltered O(n) fetch at buildPreview — fine v1, consider date-bounded predicate for large volumes. First CI green confirmed: Tests run 16 on commit 56edc99 (historic: all 15 prior runs FAILED at Xcode platform install step). Next: T10 (now unblocked; product decision resolved in this loop).

**Note (2026-07-09, loop 11 T10):** B12 fixed via T10 (Balance edits as explicit adjustments): Account edit now calls `AccountBalanceService.applyBalanceEdit(oldBalance:newBalance:to:context:)` instead of directly writing account.balance. The helper creates a "Balance adjustment" Transaction (nil category, income-typed if delta>0 else expense, abs(delta) amount) and applies it, yielding exactly the new balance. Excludes isAdjustment transactions from all income/expense/cash-flow/savings-rate calculations (12 summation sites verified: MonthlyReviewCalculator income/expenses/topCategories/budgetOverruns, Budget.spent, MetricsView totalIncome/totalExpenses/expensesByCategory/cashFlowData, DashboardView monthlyIncome/monthlyExpenses, ForecastEngine.averageDailyNetFlow, SafeSpendCalculator.averageDailyExpenses). CRITICAL nil-category trap: nil-category budgets catch uncategorized spend, so an adjustment expense would count against them and fire BudgetAlertCoordinator alerts on account edits (would be a user-visible surprise for a correction transaction). Deliberately INCLUDE adjustments at NetWorthCalculator (adjustments ARE part of balance history; at() must reverse them to reconstruct historical balances correctly), CashFlowCalendarView (day sums), TransactionsListView.dailyNetTotal (headers must add up to visible rows), CSVImportView (parsed rows). Code Review found a pre-existing bug during refactor: `BudgetDetailView.periodTransactions` was hand-rolling a duplicate of `Budget.spent` predicate without the !isAdjustment guard, causing nil-category budget detail screens to show contradictory data (hero spent count excluded adjustments, but chart/list included them). Implementer extracted both into `Budget.matchingTransactions(from:start:end:)` (single source of truth). Code Review explicitly noted this refactor also fixed the old `periodTransactions` returning ALL expenses for nil-category budgets (not just uncategorized), realigning with documented CLAUDE.md "nil = catch-uncategorized" semantics — ruled acceptable as a bug fix against resolved semantics, not a new product decision; commit message must name both fixes. QA PASS: TEST SUCCEEDED 319/319 (311→319 from T9). Follow-ups filed: (1) nil-category matchingTransactions realignment test (categorized real expense excluded); (2) AccountBalanceService.applyBalanceEdit has single AddAccountView caller — future second callers must capture oldBalance before any mutation to avoid double-application; (3) unparseable-balance-text risk: AmountParser.parse ?? 0 previously silent-zeroed, now creates visible full-delta adjustment (improvement, new surface); consider form guard against parse failures (UX); (4) type-flip (asset↔liability) edits yield numerically-correct but odd-looking adjustment ledger entries (accepted trade-off, product-copy consideration).

**Note (2026-07-09, loop 12 T12):** B6 fixed via T12 (Display-currency abstraction): new `EggSpend/Utilities/CurrencyFormat.swift` with locale-derived accessors (code/symbol/style/money methods) + nonisolated(unsafe) static override var for future Settings toggle. Inventory corrected: 78 hard-coded-USD sites (up from audit's 76, with T8/T10 adding two to the ledger side). Implementer-found bug in round 1: symbol(locale:) lacked .locale(_) when constructing NumberFormatter, so style(locale:) calls would format with process-locale separators instead of the override locale — caught by de_DE test failing. Fixed in round 2: symbol(locale:) now honors override via NumberFormatter(locale:, currencyCode: override).currencySymbol ?? override fallback. 78 mechanical .currency(code: CurrencyFormat.code) swaps + 9 Text("$") prefix swaps; CompactCurrencyAxisFormatter default unchanged (accepted-imprecision comment added for symbol-prefix layout + English K/M/B/T suffixes in postfix-symbol locales like de_DE/fr_FR/ja_JP). R9 NEW: CurrencyFormat.override is a bare static var with no SwiftUI dependency edge — future Settings toggle must add @Observable/notification wiring to trigger re-render; currently requires app restart. QA PASS round 2: TEST SUCCEEDED 331/331 (319→331 from T10). Code Review round 1 flagged real defect: symbol(locale:) ignored override while code/style/money honored it, creating two disagreeing override points. Implementer round 2 fixed; Code Review re-verdict APPROVE (independently reproduced ICU values: en_US+GBP → £, de_DE+JPY → ¥). Follow-ups filed: (1) R9: Settings toggle re-render wiring (@Observable/notification); (2) SafeToSpendView prose "$0"/"$500" — localize when full locale-string coverage desired; (3) CompactCurrencyAxisFormatter symbol-prefix layout accepted v1 imprecision for postfix-symbol locales; (4) optional DRY consistency: migrate bare .currency(code: CurrencyFormat.code) sites to CurrencyFormat.style/money (reviewer judged split reasonable).

**Note (2026-07-09, loop 13 T13a):** R4 (No data export/portability) partially addressed via T13a (DataExporter engine): new `EggSpend/Utilities/DataExporter.swift` provides CSV export (transactions/accounts/transfers with RFC-4180 escaping, POSIX formatting, signedAmount precision) and versioned full-backup JSON (BackupEnvelope with schemaVersion=1 + 7 Codable DTOs covering all app models) + validateBackup for schema-version checking (restore application out of scope for this loop). DTO design supports round-trip integrity (dates as fractional-seconds ISO8601, amounts with sign convention matching CSVParser inference). CSVParser auto-detect columns unmodified; exports include extra Account column as groundwork for future mapping presets. Multi-account export limitation confirmed: CSV import applies single selected account per session, so re-importing a multi-account export cannot fully dedupe in one pass — documented as named asserted test (testTransactionsCSVMultiAccountExportDoesNotFullyDedupeWithSingleAccountReimport), not a silent gap. 18 new unit tests (testDataExporterTests.swift) all pass; QA PASS 349/349. R4 UI exposure (Settings screen, ShareLink/fileExporter wiring) deferred to T13b. Follow-ups: (1) optional guard csvAmountString against NaN/Infinity (theoretical); (2) optional CSV round-trip test for special chars in category/account names (low risk, same csvField helper); (3) optional cache Bundle.main.infoDictionary if hot-path identified.

**Note (2026-07-09, loop 14 T13b):** R4 FULLY FIXED via T13b (Settings screen + export entry point): new `EggSpend/Views/Settings/SettingsView.swift` presents four ShareLink export actions (transactions CSV, accounts CSV, transfers CSV, full JSON backup) via a gear-icon entry point on DashboardView toolbar. Payload assembly via seven @Query arrays ensures fresh data per share. DataExporter.exportFilename(prefix:ext:date:) promoted helper enforces POSIX yyyy-MM-dd format (locale-independent, slash-free for filename safety per T5 locale lesson). QA round 1 PASS (350/350) but empirically reproduced a REAL defect: lazy generate closures captured live @Model arrays and crossed Transferable's @Sendable async boundary; CoreTransferable can invoke exporting closures off-main-actor, and DataExporter walks SwiftData relationships (fault-loading) — off-main access can crash or corrupt. PROCESS NOTE: QA's round-1 claim of "zero warnings" was a false negative — the two non-Sendable warnings ARE real, but round 1's grep likely hit an incremental-build log rather than a clean build. Code Review round 1 confirmed defect and required eager Sendable payloads. Implementer round 2: CSVExportFile/JSONExportFile now store plain let content/data; payloads built eagerly in computed properties (guaranteed MainActor); exporting closures wrap already-computed values only. Isolated typecheck + full build → zero warnings; 19/19 targeted tests pass. QA round 2: full suite 350/350, zero warnings on clean build. Code Review re-verdict APPROVE. R2 (SyncStatus surfacing) and R9 (currency-override re-render wiring) explicitly NOT absorbed by T13b — SettingsView is their dedicated extensible host structure (bare Form with "Data" section, additional sections appended by future tasks without dead UI rows today). Both remain open items with Task column updated to note SettingsView as their assigned host. Follow-up: QA process note filed (warning checks must use clean builds, not incremental).

**Note (2026-07-09, loop 15 T14):** T14 (BalanceSnapshot history model) adds per-account daily balance snapshots via write-on-launch and scenePhase/.active handlers (update-in-place same-day deduplication, startOfDay normalization, injectable date/calendar for testable day-rollover). NetWorthCalculator.at(date:accounts:transactions:) extended with additive snapshot-aware overload: snapshots win over reconstruction when both exist for same date (exact balance recorded at capture time, eliminating reconstruction gaps). Latest-createdAt duplicate tolerance: CloudKit cross-device sync races can duplicate BalanceSnapshot rows (no unique constraints available in SwiftData); duplicates tolerated on read via uniquingKeysWith first-wins strategy (not swept/cleaned), per T23 adjacency note. QA found and implementer fixed: Dictionary(uniqueKeysWithValues:) TRAPS on duplicate accountIDs — the same CloudKit sync condition feature explicitly tolerates (would crash MetricsView timeline on each launch if duplicate-id Account synced); replaced with uniquingKeysWith first-wins + comment. B21 (transfer-boundary) impact: snapshot-based approach naturally bounds the damage for dates when app was launched (snapshots record real balances directly), but pre-snapshot dates still rely on reconstruction (which remains invisible to transfers between included/excluded accounts). B21 status updated to "open (bounded by T14)" — snapshots limit historical-accuracy gaps to the pre-launch period. QA PASS 360/360 (350→360 new tests). Code Review APPROVE zero required fixes + 2 optional follow-ups: (1) add test pinning snapshot-path liability sign math for an INCLUDED liability (second independent sign-ternary implementation in money code); (2) add one-line scenePhase comment documenting that double-fire with onAppear is self-healing via idempotence. Final suite re-run: expected 360/360 (commit pending suite confirmation).

**Note (2026-07-09, loop 16 T15):** B16 fixed via T15 (Forecast/SafeSpend double-count fix): Repo Analyst finding revealed backlog's step 1 was stale — RecurringProjection.occurrences already emits starting from item.nextDueDate (not fast-forwarding from window start), and post-T4 processing guarantees active items' nextDueDate is in the future, so materialized transactions are never re-projected. The REAL double-count: generated recurring transactions contribute to averageDailyNetFlow's 60-day organic-activity drift AND simultaneously get re-added as explicit upcomingEvents lump-sum projections — the same historical materialized amount plus the forward-projected amount counted twice. Backlog's step 2 rejected with documented rationale: "drop occurrences <= now" would hide mid-session-created unmaterialized bills from the calendar UI (processRecurringTransactions runs only at launch, not mid-session), and buildForecast's day buckets already start at day 1 anyway. ForecastEngine.averageDailyNetFlow += filter !$0.isGenerated (one-token exclusion) so drift baseline reflects only organic transactions; upcomingEvents remains the sole recurring-projection source. SafeSpend determination: upcomingNetOutflowReserve consumes upcomingEvents (unaffected); averageDailyExpenses deliberately retains generated history (no paired recurring add-back — excluding would understate the safety buffer); acceptance wording read as overall forecast accuracy, not a literal reserve change. ASSUMPTION comments rewritten to document: organic-only drift, recurring solely via explicit events, paused/expired items' generated history intentionally vanishes (ended obligations shouldn't project). RecurringProjection and SafeSpendCalculator updated comment-only to flag the asymmetry and warn against a <= now drop. 4 new tests (testAverageDailyNetFlowExcludesGeneratedTransactions mixed fixture, testUpcomingEventsIncludesUnmaterializedItemDueToday pins rejection, testBuildForecastDoesNotDoubleCountMaterializedRecurring acceptance trace, testUpcomingNetOutflowReserveIgnoresGeneratedTransactionHistory non-tautological regression). QA PASS 364/364 (360→364, zero warnings, comment-only diffs verified, acceptance hand-traced). Code Review APPROVE zero required fixes; traced paused/expired semantics (cancelled subscriptions correctly stop depressing forecast), amount-change case (strictly better), edited-generated-transaction case (stays excluded — defensible), failed-launch-processing edge (occurrences emit only from window start — graceful, no double count). Optional follow-up: one-clause comment addendum noting user-edited generated transactions remain excluded from drift.

**Note (2026-07-09, loop 19 T17a):** T17a (Auto-categorization rule engine) adds no bug fixes — feature-gap work enabling T17b UI hooks. No register items closed or opened this loop. Follow-up: empty-pattern defensive guard added to FEATURE_BACKLOG inbox (currently unreachable per AddTransactionView title validation + CSV "Row N" fallback, but good defensive practice for future callers).
